import styles from '../../../styles/Home.module.css'

export function HomeHeader() {
    return (
        <div>
            <a href="Home" className={styles.homebutton}>Entrar</a>
        </div>
    );
}